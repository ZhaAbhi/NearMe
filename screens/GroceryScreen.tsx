import React from 'react';
import {SafeAreaView, Text, View} from 'react-native';

const GroceryScreen = (): JSX.Element => {
  return (
    <SafeAreaView>
      <Text>This is the Grocery Screen</Text>
    </SafeAreaView>
  );
};

export default GroceryScreen;
