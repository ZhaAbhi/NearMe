import React from 'react';
import {SafeAreaView, Text, View} from 'react-native';

const GardeningScreen = (): JSX.Element => {
  return (
    <SafeAreaView>
      <Text>This is the Gardening Screen</Text>
    </SafeAreaView>
  );
};

export default GardeningScreen;
